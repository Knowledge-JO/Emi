// TODO: drizzle-kit config — dialect 'postgresql', schema './src/database/schema',
// out './src/database/migrations', credentials from DATABASE_URL. The pgvector extension must be
// enabled in a first migration before any vector column is created.
