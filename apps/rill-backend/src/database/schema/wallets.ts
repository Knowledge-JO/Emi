// TODO: wallets table — Altana smart accounts for both users and agents. Stores address, owner
// kind (user | agent), signer kind (local key | passkey), chains provisioned, and whether the
// first admin-signed tx has registered the admin key in Keystore. Never stores private keys.
