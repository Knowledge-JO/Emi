// TODO: jobs table — ERC-8183 escrow jobs. Hirer (user or agent), worker agent, spec, escrow
// amount and token, on-chain job id, and state (created → funded → accepted → delivered →
// settled | disputed | cancelled).
