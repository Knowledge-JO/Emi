// TODO: events table — append-only event store. Every state transition in the system lands here
// (intent parsed, agent selected, session granted, payment settled, job delivered, session
// revoked) so any run is auditable and replayable.
