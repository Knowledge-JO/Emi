// TODO: authorizations table — the on-chain grant receipt: wallet, session public key, grant tx
// hash, Keystore registration fee, expiry, and revocation tx. This is the audit trail for
// "who was allowed to do what, when".
