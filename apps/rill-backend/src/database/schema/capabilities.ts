// TODO: capabilities table — the central marketplace object. A machine-matchable declaration:
// name, supported assets, supported protocols, input/output schema, pricing model
// (per_job | per_request | subscription), unit price, and settlement rail (x402 | erc8183).
// An embedding column lets the capability graph match goal nodes semantically.
