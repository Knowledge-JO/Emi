// TODO: permissions table — the exact scope committed at grant time: contract allowlist (calls)
// and spend caps (limit, period, token), stored as strings so bigints survive round-tripping.
// Execute-time validation requires a byte-exact match with the on-chain authorization.
