import { Injectable } from '@nestjs/common';

// TODO: Request/response logging with a correlation id that follows an intent through planning,
// agent commerce and on-chain execution. Redact private keys, session secrets and payment headers.
@Injectable()
export class LoggingInterceptor {}
