import { Injectable } from '@nestjs/common';

// TODO: Browser onboarding and recovery — createPasskeyWallet({ name, rpId }) and
// recoverFromPasskey({ rpId }). No seed phrases, no server state, no localStorage required.
@Injectable()
export class PasskeyService {}
