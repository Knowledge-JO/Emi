// TODO: Single Altana client provider: createClient({ chains: [BNB] }) — add ETHEREUM/BASE only
// when cross-chain verification is needed. The same wallet address is provisioned on every listed
// chain; select per operation with chainId.
