import { Injectable } from '@nestjs/common';

// TODO: Raise and resolve disputes over rejected deliverables, and apply the outcome to both
// parties' reputation. Timeouts must have a defined default so escrow can never be stuck forever.
@Injectable()
export class DisputeService {}
