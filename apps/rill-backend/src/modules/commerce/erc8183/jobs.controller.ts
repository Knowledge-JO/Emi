import { Controller } from '@nestjs/common';

// TODO: Job lifecycle API, usable by users hiring agents AND by agents hiring agents — create,
// accept, submit deliverable, settle, dispute, read history.
@Controller('jobs')
export class JobsController {}
