import { Injectable } from '@nestjs/common';

// TODO: Dispatch a step to its agent over the right rail — an HTTP capability call paid via x402,
// an escrow job created via ERC-8183, or a direct protocol execution through the execution layer.
@Injectable()
export class AgentRouterService {}
