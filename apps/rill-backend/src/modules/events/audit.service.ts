import { Injectable } from '@nestjs/common';

// TODO: Human-readable audit trail: for a given intent, what was planned, which agents were
// hired, what authority was granted, what was paid, and what executed on-chain.
@Injectable()
export class AuditService {}
