import { Injectable } from '@nestjs/common';

// TODO: Intent lifecycle: persist the raw goal, run the parser, build the capability graph, and
// hand the graph to the orchestrator.
@Injectable()
export class IntentService {}
