import { Injectable } from '@nestjs/common';

// TODO: Start, signal, query and cancel Temporal workflows, keyed by the workflows table so a run
// can always be traced back to the intent that created it.
@Injectable()
export class WorkflowRunnerService {}
