import { Injectable } from '@nestjs/common';

// TODO: Read/write the ERC-8004 registry — register an agent identity, resolve one by id or
// address, and sync on-chain records into agent_identities from a tracked block height.
@Injectable()
export class Erc8004RegistryService {}
