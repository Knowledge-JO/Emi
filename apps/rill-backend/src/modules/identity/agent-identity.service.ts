import { Injectable } from '@nestjs/common';

// TODO: Bind a marketplace listing to its on-chain identity and its agent wallet, and verify the
// binding: the agent's declared address must control the wallet it claims.
@Injectable()
export class AgentIdentityService {}
