import { Module } from '@nestjs/common';

// TODO: ERC-8004 agent identity and discovery layer. Identity feeds commerce:
//   ERC-8004 → agent identity → marketplace → ERC-8183 / x402
@Module({})
export class IdentityModule {}
