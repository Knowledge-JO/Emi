import { Controller } from '@nestjs/common';

// TODO: Read an agent's on-chain identity: capabilities, endpoint, developer, reputation and
// commerce history. Lets any counterparty verify who they are about to hire.
@Controller('identities')
export class IdentityController {}
