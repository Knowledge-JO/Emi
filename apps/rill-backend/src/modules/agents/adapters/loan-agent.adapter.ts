import { Injectable } from '@nestjs/common';

// TODO: Loan agent (Loan Guardian) — monitor an Aave position and repay when needed. The
// canonical hiring agent: it buys risk data via x402 and hires a swap agent via ERC-8183 rather
// than implementing either itself.
@Injectable()
export class LoanAgentAdapter {}
