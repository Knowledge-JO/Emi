import { Module } from '@nestjs/common';

// TODO: The agent network — swap, loan, risk, liquidity, token and research agents. Each is an
// economic actor: it may buy capabilities via x402 and hire peers via ERC-8183 rather than
// containing every capability itself.
@Module({})
export class AgentsModule {}
