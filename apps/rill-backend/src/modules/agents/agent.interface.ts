// TODO: The contract every agent adapter implements: declared capabilities, an input schema, a
// price quote, and an execute(step, session) entry point. Adapters receive a scoped session —
// never an admin signer.
