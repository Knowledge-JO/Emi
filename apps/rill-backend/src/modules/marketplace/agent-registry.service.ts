import { Injectable } from '@nestjs/common';

// TODO: Agent listing lifecycle — register, update, suspend, delist. A listing is only valid with
// a resolvable ERC-8004 identity and at least one declared capability.
@Injectable()
export class AgentRegistryService {}
