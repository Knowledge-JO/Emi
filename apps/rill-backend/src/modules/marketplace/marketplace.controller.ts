import { Controller } from '@nestjs/common';

// TODO: Public marketplace API — browse and search agents, read an agent's capabilities, pricing
// and reputation, and resolve "which agents can do X". Callable by humans and by agents.
@Controller('marketplace')
export class MarketplaceController {}
